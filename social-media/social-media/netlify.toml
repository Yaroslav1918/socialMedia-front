[build]
  # Build command
  command = "npm install --legacy-peer-deps && ng build --prod"
  # Directory where your built project is located
   publish = "www"

# Redirects can be added here if needed
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
